name: Workflow 5 - Depth Test

on:
  workflow_call:

jobs:
  print-name:
    runs-on: ubuntu-latest
    steps:
      - name: Print workflow name
        run: |
          echo "================================"
          echo "This is Workflow 5 - Fifth level"
          echo "================================"
  
  call-next-workflow:
    needs: print-name
    uses: ./.github/workflows/workflow6-depth-test.yml
